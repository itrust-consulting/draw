# For more details on the format and content:
# https://github.com/robocorp/rcc/blob/master/docs/recipes.md#what-is-in-robotyaml

tasks:
  # Task names here are used when executing the bots, so renaming these is recommended.
  Run the robot:
    # Past failures. Check in next run:
    #shell: python -m robot --report report.html --outputdir output --logtitle "Open/Import Excel Testing" testSuit1/taskOpenExportSave.robot 
    #shell: python -m robot --report report.html --outputdir output --logtitle "Open/Import Excel Testing" testSuit1/taskImportExcelCreateAssetImport.robot
    #shell: python -m robot --report report.html --outputdir output --logtitle "Open/Import Excel Testing" testSuit1/taskMergeTSAndGraphAssets.robot
    #shell: python -m robot --report report.html --outputdir output --logtitle "Open/Import Excel Testing" testSuit1/taskMergeTSAndGraphAssets.robot
    shell: python -m robot --report report.html --outputdir output --logtitle "Open/Import Excel Testing" testSuit1/taskOpenSave.robot
    #shell: python -m robot --report report.html --outputdir output --logtitle "Open/Import Excel Testing" testSuit1/taskTestCollections.robot
    #shell: python -m robot --report report.html --outputdir output --logtitle "Open/Import Excel Testing" testSuit1/taskCompareEstimations.robot
    #shell: python -m robot --report report.html --outputdir output --logtitle "Open/Import Excel Testing" testSuit1/taskExportToTS1.robot
    #shell: python -m robot --report report.html --outputdir output --logtitle "Open/Import Excel Testing" testSuit1/taskExportToTS2.robot
    #shell: python -m robot --report report.html --outputdir output --logtitle "Open/Import Excel Testing" testSuit1

condaConfigFile: conda.yaml

environmentConfigs:
  - environment_windows_amd64_freeze.yaml
  - environment_linux_amd64_freeze.yaml
  - environment_darwin_amd64_freeze.yaml
  - conda.yaml

artifactsDir: output  

PATH:
  - .

PYTHONPATH:
  - .

ignoreFiles:
  - .gitignore
